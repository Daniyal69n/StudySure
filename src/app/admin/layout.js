export const metadata = {
  title: 'StudySure Admin Dashboard',
  description: 'Content management dashboard for StudySure',
};

export default function AdminLayout({ children }) {
  return children;
}
